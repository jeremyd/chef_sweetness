set daemon 30 # Poll at 2-minute intervals

check process skyped
           with pidfile "<%= @users_home %>/skyped/skyped.pid"
           start program = "<%= @users_home %>/bin/skyped-start.sh"
           stop = "/bin/true" # no stop program yet ..
